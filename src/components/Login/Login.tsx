import LoginContainer from "./LoginContainer";

const Login = () => {
  return (
    <div className="bg-cover bg-background-login w-screen h-screen">
      <LoginContainer />
    </div>
  );
};

export default Login;
